__pycache__/
*.pyc
.venv/
venv/
.DS_Store
/tmp/
*.egg-info/
.pytest_cache/
# Secrets (e.g. GOOGLE_API_KEY for the curator's LiteLLM model backend) --
# never committed. See curator/model.py's resolve_google_api_key().
.env
# Generated / downloaded data artifacts (exports, split CSVs, loaded YAML, …)
/data/*
!/data/.gitkeep
# Secrets — never commit API keys. Local env files only.
.env
.env.*
!.env.example
